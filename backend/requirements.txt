fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
sqlalchemy==2.0.50
pytest==8.3.4
httpx==0.28.1
ruff==0.9.2

# Real detector (MODEL_NAME=yolo). CPU-only torch is installed separately in the
# Dockerfile from the PyTorch CPU index to avoid pulling large CUDA wheels.
ultralytics==8.3.58
